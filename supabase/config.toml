project_id = "ntnzhhorrtwtejwptbxw"

[auth]
site_url = "https://big-board-redraft.vercel.app"
additional_redirect_urls = [
  "https://big-board-redraft.vercel.app/auth/callback",
  "http://localhost:3000/auth/callback",
]

[auth.email]
enable_signup = true
# Personal tool: skip the confirm-email roundtrip so password signup works
# immediately and "Account created. You can sign in now." is true.
enable_confirmations = false
